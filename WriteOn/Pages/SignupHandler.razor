@page "/signuphandler"

@using WriteOn.Shared;

@inject NewUserState NewUserState
@inject NavigationManager NavigationManager

@code {
    // FOR TESTING
    protected override void OnInitialized()
    {
        try
        {
            NavigationManager.NavigateTo(NavigationManager.BaseUri + "/home");
        }
        catch (Exception) {}
    }

    // TODO: Enter new user in database after validation
}
