@page "/loginhandler"

@using WriteOn.Shared;

@inject CurrentUserState CurrentUserState
@inject NavigationManager NavigationManager

@code {
    // FOR TESTING
    protected override void OnInitialized()
    {
        try
        {
            NavigationManager.NavigateTo(NavigationManager.BaseUri + "home");
        }
        catch (Exception) {}
    }

    // TODO: Validate user credentials using the database
}
